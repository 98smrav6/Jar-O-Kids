<article>
  <a href="/parentDash"> &larr; Back to Parent Dashboard</a>
  <h2>
    Edit Student
  </h2>
  <form class="edit-student-form">
    <div class= "parent-edit edit-student">
        <h2>Student First Name:{{student.student_firstname}}</h2>
        <h2>Student Last Name: {{student.student_lastname}}</h2>
        <p>Student Address: {{student.student_address}}</p>
        <p>Student Grade: {{student.student_grade}}</p>
    </div>
    
    {{!-- DIV for Changing Enrollment Status, IF the same dashboard is to be used by Admin to avoid duplicate code -ECM 
    
    <div class= "admin-edit edit-student">
        <p>Student Enrollment Status:  {{student.student_status}}</p>
    </div> 
    
    --}}
    <button type="submit">Save post</button>
    <button type="button" class="delete-post-btn">Delete post</button>
  </form>
</article>
